import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#264f9e',
    },
    secondary: {
      main: '#455fb4',
    },
    background: {
      paper: '#2d56a5',
    },
  },
});

export default theme;
