import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#bee3db",
    },
    secondary: {
      main: "#ffd6ba",
    },
    background: {
      //   default: "#555b6e",
      paper: "#89b0ae",
    },
  },
});

export default theme;
