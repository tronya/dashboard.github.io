export const DRAWER_WIDTH = 240;

export enum ScreenType {
  DESKTOP = 'DESKTOP',
  TABLET = 'TABLET',
  MOBILE = 'MOBILE',
  INITIAL = 'INITIAL',
}

export enum AnchorType {
  RIGHT = 'right',
  LEFT = 'left',
}
